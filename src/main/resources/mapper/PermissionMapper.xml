<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pannfmis.dao.PermissionMapper">

    <!-- 权限表结果映射 -->
    <resultMap id="PermissionResultMap" type="org.pannfmis.entity.Permission">
        <id property="permissionId" column="permission_id" />
        <result property="permissionCode" column="permission_code" />
        <result property="permissionName" column="permission_name" />
        <result property="description" column="description" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <!-- 根据权限ID查询 -->
    <select id="selectByPermissionId" parameterType="java.lang.Long" resultMap="PermissionResultMap">
        SELECT permission_id, permission_code, permission_name, description, created_at, updated_at
        FROM permissions WHERE permission_id = #{permissionId}
    </select>

    <!-- 根据权限代码查询 -->
    <select id="selectByPermissionCode" parameterType="java.lang.String" resultMap="PermissionResultMap">
        SELECT permission_id, permission_code, permission_name, description, created_at, updated_at
        FROM permissions WHERE permission_code = #{permissionCode}
    </select>

    <!-- 查询所有权限 -->
    <select id="selectAllPermissions" resultMap="PermissionResultMap">
        SELECT permission_id, permission_code, permission_name, description, created_at, updated_at
        FROM permissions ORDER BY permission_id
    </select>

    <!-- 插入权限 -->
    <insert id="insertPermission" parameterType="org.pannfmis.entity.Permission" useGeneratedKeys="true" keyProperty="permissionId">
        INSERT INTO permissions (permission_code, permission_name, description, created_at, updated_at)
        VALUES (#{permissionCode}, #{permissionName}, #{description}, NOW(), NOW())
    </insert>

    <!-- 更新权限 -->
    <update id="updatePermission" parameterType="org.pannfmis.entity.Permission">
        UPDATE permissions SET
                               permission_code = #{permissionCode},
                               permission_name = #{permissionName},
                               description = #{description},
                               updated_at = NOW()
        WHERE permission_id = #{permissionId}
    </update>

    <!-- 删除权限 -->
    <delete id="deleteByPermissionId" parameterType="java.lang.Long">
        DELETE FROM permissions WHERE permission_id = #{permissionId}
    </delete>

</mapper>