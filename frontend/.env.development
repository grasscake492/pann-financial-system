# .env.development（开发环境）
# 启用MSW拦截请求（关键：true=拦截，false=走后端）
VITE_USE_MSW=true
# 后端代理地址（仅VITE_USE_MSW=false时生效）
VITE_API_TARGET=http://localhost:8080/pann-financial-system
# 请求基础路径（和MSW的API前缀对齐）
VITE_APP_BASE_API=/api
# MSW的API前缀（可选，和VITE_APP_BASE_API保持一致）
VITE_API_BASE=/api